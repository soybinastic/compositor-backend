from django.apps import AppConfig


class StudioSessionsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.sessions'
    label = 'studio_sessions'
    verbose_name = 'Studio Sessions'
